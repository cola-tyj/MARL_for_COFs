# COF 对称分子数据集——构建流程详解

**日期**: 2026-08-08 | **分支**: `feat/hierarchical-opt` | **数据集版本**: v2.0

---

## 目录

1. [整体架构](#一整体架构)
2. [Stage 1: 2D 拓扑组合生成](#二stage-1-2d-拓扑组合生成)
3. [Stage 2: 3D 构象嵌入](#三stage-2-3d-构象嵌入)
4. [Stage 3: 几何优化与对称破缺筛查](#四stage-3-几何优化与对称破缺筛查)
5. [Stage 4: 数据导出](#五stage-4-数据导出)
6. [模板库详解](#六模板库详解)
7. [层级核心优化](#七层级核心优化)
8. [数据集规模与统计](#八数据集规模与统计)
9. [文件索引](#九文件索引)

---

## 一、整体架构

### 设计目标

构建一个**点群对称性可标签化**的 COF 中间体核心分子数据集。每个分子经历"2D 拓扑生成 → 3D 几何嵌入 → 力场松弛 → 对称性验证"四阶段筛选。

### 数据流

```
config.py (模板库: 511 核心 × 82 臂)
    │
    ▼
Stage 1: generator.py          ─── 2D 分子 (SMILES, 拓扑对称)
    │                                方法: ReplaceSubstructs + _cleanup_bridges
    │                                原理: * 位于图自同构轨道的等效位置
    │                                      ReplaceSubstructs 等价替换 → 拓扑对称保持
    │
    ▼
Stage 2: relaxer.py (Embedder) ─── 3D 构象 (含 H, ETKDGv3 坐标)
    │                                方法: ETKDGv3 + 8 种子多构象选优
    │
    ▼
Stage 3: relaxer.py (GeometryRelaxer + SymmetryAnalyzer)
    │                                方法: MMFF94 力场优化 → pymatgen PointGroupAnalyzer
    │                                筛选: EMBED_FAIL / RELAX_FAIL / ENERGY_FAIL / SYMMETRY_BREAK
    │
    ▼
Stage 4: main.py               ─── CSV + XYZ + PyTorch Dataset
                                      输出: cof_dataset_3d_pass.csv (2,601 分子)
```

### 核心设计原则

1. **对称性由构造保证**: `*` 位于图自同构轨道的等效位置，ReplaceSubstructs 等价替换 → 2D 拓扑对称 = 核心对称
2. **3D 验证不可省略**: 柔性旋转自由度可导致对称破缺（C₃ → C₁），需 pymatgen 点群验证
3. **模块化可扩展**: 新增核心/臂只需在 `config.py` 添加条目

---

## 二、Stage 1: 2D 拓扑组合生成

### 负责文件

| 文件 | 作用 |
|------|------|
| `config.py` | 定义核心模板库（CORE_TEMPLATES）和臂库（ARM_LIBRARY） |
| `generator.py` | 组合合成引擎：核心 + 臂 → 完整分子 |
| `generator.py` → `_cleanup_bridges()` | 移除 ReplaceSubstructs 产生的桥接 dummy 原子 |

### 算法步骤

**Step 1: ReplaceSubstructs**
```python
products = AllChem.ReplaceSubstructs(core_mol, query='*', arm_mol, replaceAll=True)
```
核心中所有 `*` 被同一个臂等价替换。产物含有桥接 `*`（degree=2）。

**Step 2: _cleanup_bridges**
```python
# 1. 找出所有桥接 * (atomic_num==0 且 degree==2)
# 2. 降序移除桥接 *
# 3. 邻居索引校正后创建 C-C 直连键
```
移除桥接 `*`，将核心锚点与臂直接连接。

**Step 3: SanitizeMol** — Kekulé 化 + 化合价验证。

### 示例

```
输入: triazine_C3 (*c1nc(*)nc(*)n1) + CHO_ph (*c1ccc(C=O)cc1)
  ↓ ReplaceSubstructs → 33原子, 3个桥接*
  ↓ _cleanup_bridges  → 30原子, 0残留*
  ↓ SanitizeMol       → ✓
输出: O=Cc1ccc(-c2nc(-c3ccc(C=O)cc3)nc(-c3ccc(C=O)cc3)n2)cc1
      2,4,6-三(4-甲酰基苯基)-1,3,5-三嗪, C₃ 对称 ✓
```

---

## 三、Stage 2: 3D 构象嵌入

### 负责文件

| 文件 | 类/函数 | 作用 |
|------|:------:|------|
| `relaxer.py` | `Embedder` | ETKDGv3 距离几何嵌入 |
| `relaxer.py` | `SymmetryAnalyzer` | pymatgen PointGroupAnalyzer + 惯性张量回退 |

### ETKDGv3 嵌入

- 加氢 → ETKDGv3 随机坐标嵌入 → MMFF94 预优化
- 8 个不同随机种子产生多构象
- 每个构象计算点群后选最佳匹配进入 Stage 3

### 点群分析

- 首选: pymatgen `PointGroupAnalyzer` (tolerance=0.3Å)
- 回退: 惯性张量特征值简并度推断
- 评分体系: 精确匹配(3.0) > 等价类(2.0) > 超群(1.0) > 数值匹配(0.5) > 破缺(-1.0)

---

## 四、Stage 3: 几何优化与对称破缺筛查

### MMFF94 力场优化

- RDKit MMFF94（默认，有机分子专用）
- 可选: UFF / MACE-OFF23 / GFN2-xTB
- 输出: 最终能量 + 最大原子位移

### 三级淘汰

| 淘汰类型 | 条件 | 含义 |
|------|------|------|
| FAIL_EMBED | ETKDGv3 返回码≠0 | 位阻过大，无法嵌入 |
| FAIL_RELAX | 力场优化异常 | 结构不稳定 |
| FAIL_ENERGY | 能量异常（>1e8 或 NaN） | 力场发散 |
| FAIL_FORCE | 原子位移 > 阈值（默认 5.0 Å） | 几何崩塌 |
| FAIL_SYMMETRY_BREAK | 松弛后点群不匹配目标 | 对称性丢失 |

### 对称破缺检测

```python
_check_symmetry(target_pg, actual_pg):
    if actual_pg in BROKEN_SYMMETRY_TAGS: return False      # C1, Cs, Ci
    if actual_pg == target_pg: return True                   # 精确匹配
    if actual_pg in PG_EQUIVALENCE[target_pg]: return True   # 等价类
    if target_pg in PG_EQUIVALENCE[actual_pg]: return True   # 超群
```

### 实战案例

**PASS (三嗪+直接醛基)**: 刚性骨架，三个-CHO 到质心距离全等(2.768Å)，夹角 120°，共面性 0.000Å → C₃ₕ。

**FAIL (三苯基苯+直接醛基)**: 核心苯环与外臂苯环间单键旋转，三个臂取了不同扭转角，共面偏差 0.103Å → C₁。

---

## 五、Stage 4: 数据导出

### 输出文件

| 文件 | 内容 |
|------|------|
| `cof_dataset_3d_pass.csv` | 最终数据集：2,601 个 3D 验证分子 |
| `augmented_dataset.csv` | 原始核心 2D 数据集（2,752 分子） |
| `xyz/*.xyz` | PASS 分子的 3D 坐标文件 |
| `pipeline_stats.json` | 运行统计摘要 |

### 数据集列名

`SMILES`, `Core`, `Arm`, `Target_PG`, `Point_Group`, `Energy_kcal_mol`, `Source`

---

## 六、模板库详解

### 核心模板来源

| 来源 | 数量 | 说明 |
|------|:--:|------|
| 原始设计 | 41 | 基于 COF 文献已知对称构建块（参考文献见 `docs/core_doc.md`） |
| 层级组装 | 414 | C₃/C₄/H₆ 节点 × C₂ 线性臂 = 18 × 23，自动生成 |
| pyCOFBuilder | 56 | 从 `pycofbuilder/data/core/` 导入（L2=28, T3=14, D4=5, S4=5, R4=3, H6=2） |
| **合计** | **511** | |

### 核心对称族分布

| 对称族 | 原始 | 层级 | pyCOFBuilder | 合计 |
|------|:--:|:--:|:--:|:--:|
| C₂ | 23 | — | 28 | 51 |
| C₃ | 13 | — | 14 | 27 |
| C₄/S₄ | 4 | — | 15 | 19 |
| H₆/D₆ₕ | 1 | — | 2 | 3 |
| 层级(C₃节点) | — | 288 | — | 288 |
| 层级(C₄节点) | — | 92 | — | 92 |
| 层级(H₆节点) | — | 23 | — | 23 |
| 层级(S₄节点) | — | 11 | — | 11 |

### 臂模板来源

| 来源 | 数量 | 说明 |
|------|:--:|------|
| 原始设计 | 75 | 官能团 + 连接子类型（直接/苯基/联苯/炔基桥） |
| pyCOFBuilder | 7 | 从 `pycofbuilder/data/conector/` 导入 |
| **合计** | **82** | |

### `*` 位置确定规则

- **核心**: `*` 位于分子图自同构轨道的等效位置（RDKit `CanonicalRankAtoms` 验证）
- **臂**: `*` 始终在连接核心的一端，官能团在外端
- **层级核心**: 父核心的每个 `*` 被 C₂ 线性核心替换 → 对称性保持

---

## 七、层级核心优化

层级核心（414 个）MW 范围 336–2425 Da，直接跑会卡死。实现了4 项优化：

| 优化 | 机制 | 效果 |
|------|------|------|
| MW 预过滤 | `max_heavy_atoms` 阈值，超限分子 Stage 1 后丢弃 | 砍掉过大的 |
| 减少构象尝试 | `num_conformer_attempts` 8→3，大分子→1 | 嵌入耗时 ×0.37 |
| 按大小排序 | 重原子数升序处理 | 渐进收割 |
| 动态调整 | 每个分子根据 n_heavy 独立设置尝试次数 | 精准分配 |

用法: `python main.py --hierarchical --max-heavy 100 --workers 8`

---

## 八、数据集规模与统计

### 最终数据集

| 文件 | 内容 |
|------|------|
| `cof_dataset_3d_pass.csv` | 3D 验证通过的分子（2,653），含能量、点群、`Dimension='3D'` |
| `cof_dataset_full.csv` | 2D + 3D 联合数据集（4,040），`Dimension` 列区分 2D/3D |
| `augmented_dataset.csv` | 原始核心 2D 分子（2,752），无 3D 坐标 |

### `cof_dataset_3d_pass.csv`

| 指标 | 值 |
|------|:--:|
| 独特分子 (SMILES) | 2,653 |
| 点群种类 | 17 |
| 平均能量 | 50.7 kcal/mol |
| 能量范围 | -623 ~ 761 kcal/mol |

### 分来源统计

| 来源 | 2D 生成 | 3D 筛查 | 3D PASS | 通过率 |
|------|:--:|:--:|:--:|:--:|
| 原始 41 核心 | 2,752 | 2,752 | 1,348 | 49.0% |
| 层级 414 核心 (v1) | 10,798 | 10,798 | 125 | 1.2% |
| 层级 414 核心 (v2) | 21,543 | 21,543 | 107 | 0.5% |
| pyCOFBuilder 56 核心 | 3,922 | 3,922 | 1,159 | 29.6% |
| **合计** | **~39,000** | — | **2,653** (去重) | |

### 点群分布

```
C₂  (979)  ████████████████████████████████████  36.9%
C₂ₕ (524)  ████████████████████                  19.8%
D₂  (309)  ████████████                          11.6%
C₂ᵥ (171)  ██████                                 6.4%
C₃  (163)  ██████                                 6.1%
C₃ₕ (155)  ██████                                 5.8%
S₄  ( 98)  ████                                   3.7%
D₂ₕ ( 59)  ██                                     2.2%
D₃  ( 44)  ██                                     1.7%
D₃ₕ ( 44)  ██                                     1.7%
其他 ( 107) ████                                   4.0%
```

### 2D/3D 标记

`cof_dataset_full.csv` 包含 `Dimension` 列：
- `'3D'`: 已通过 3D 对称性验证，含 `Point_Group` 和 `Energy_kcal_mol` 数据（2,653 个）
- `'2D'`: 仅 2D 拓扑生成，尚未通过 3D 验证（1,387 个）

### 各核心产率 (TOP 10)

| 核心 | PASS 分子数 |
|------|:--:|
| pb_BDFN_L2 (苯并二呋喃) | 73 |
| pb_BBTZ_L2 (苯并二噻唑) | 72 |
| pb_BENZ_L2 (苯) | 71 |
| pb_BDTP_L2 (苯并二噻吩) | 69 |
| pb_ANTR_L2 (蒽) | 68 |
| pb_3BPD_L2 (3,3'-联吡啶) | 68 |
| biphenyl_C2 | 65 |
| pb_PYRN_L2 (芘) | 63 |
| azobenzene_C2 | 60 |
| pb_TIDA_L2 | 56 |

---

## 九、文件索引

### 核心代码

| 文件 | 行数 | 作用 |
|------|:--:|------|
| `config.py` | ~4,100 | 511 核心 + 82 臂模板库 + 运行参数 |
| `generator.py` | ~260 | Stage 1：2D 组合生成 + 桥接清理 |
| `relaxer.py` | ~660 | Stage 2-3：3D 嵌入 + 点群分析 + 力场松弛 |
| `main.py` | ~800 | CLI 入口 + 多进程编排 + 层级核心优化 |
| `dataset.py` | ~320 | PyTorch Dataset / DataLoader |
| `validate_augmented.py` | ~200 | 独立 3D 验证工具 |

### 数据文件

| 文件 | 内容 |
|------|------|
| `output/cof_dataset_3d_pass.csv` | 最终数据集（2,601 分子） |
| `output/augmented_dataset.csv` | 原始核心 2D 数据集 |
| `output/augmented_3d_pass.csv` | 原始核心数据增强 PASS（135） |
| `output/hierarchical/hierarchical_cores_dataset.csv` | 层级核心筛查结果 |
| `output/pycofbuilder/pycofbuilder_cores_dataset.csv` | pyCOFBuilder 筛查结果 |

### 工具与文档

| 文件 | 作用 |
|------|------|
| `tools/import_pycofbuilder.py` | 从 pyCOFBuilder 导入核心和臂 |
| `visualization/visualize.py` | 8 张可视化图表 |
| `docs/数据集构建完整流程.md` | 本文档 |
| `docs/core_doc.md` | 41 个原始核心的参考文献 |
| `docs/interaction_log.md` | 完整交互记录 |
| `README.md` | 项目入口文档 |
