# MARL_for_COFs 工作上下文规则

本仓库后续任务默认先复用 [项目静态上下文](docs/项目静态上下文.md)，不要每次重新扫描
全部文档、源码、历史实验或运行目录。当前进度只从
[项目动态状态](docs/项目动态状态.md) 开始按需追踪。

## 每次任务的上下文预告

在读取文件或执行命令前，先用一段简短中文说明：

- **复用的静态上下文**：本次直接采用哪些既有约束；
- **新读取的动态信息**：为本次任务需要读取哪些改动、日志、结果或相关源码；
- **无需重复读取**：哪些历史文档、无关模块或已冻结数据不需要再读。

预告应具体但简短。随后直接工作，不因这项规则额外询问用户。

## 按需读取规则

1. 先读与请求直接相关的文件；禁止为“熟悉项目”而重复遍历整个仓库。
2. 只有发生以下情况才重新读取动态内容：新需求、相关代码/文件修改、报错日志、新测试或
   运行结果、数据 manifest/schema/hash 变化、依赖/环境变化，或当前工作树可能与目标重叠。
3. 旧实验细节默认不读。只有用户要求追溯决策或当前问题直接依赖旧证据时，才读取
   `docs/history/`、SemlaFlow/MiDi 历史报告或旧日志。
4. 修改代码前仍须读取目标文件及其直接依赖；“复用静态上下文”不能替代对即将编辑代码的
   精确检查。
5. 完成产生状态变化的任务后，精简更新 `docs/项目动态状态.md`；只有长期约束真的改变时才
   更新 `docs/项目静态上下文.md`。

## 信息优先级

当前用户要求 > 实际代码/数据/manifest > 最新运行报告与动态状态 > 静态上下文 > 历史文档。
若彼此冲突，不静默选择：以实际数据和代码核验，并在结果中指出差异。

